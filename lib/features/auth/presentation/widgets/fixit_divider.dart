import 'package:flutter/material.dart';

class FixitDivider extends StatelessWidget {
  const FixitDivider({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Divider(
      color: Color(0XFFEDEDEF),
      thickness: 1,
    );
  }
}
