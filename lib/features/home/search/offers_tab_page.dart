import 'package:flutter/material.dart';

class SearchOffersPage extends StatelessWidget {
  const SearchOffersPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Offers'),
    );
  }
}
